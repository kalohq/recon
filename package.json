{
  "private": true,
  "scripts": {
    "lint": "eslint **/src/**",
    "test": "npm run lint && npm run typecheck && mocha **/lib/**/*.test.js",
    "typecheck": "flow check --all",
    "build": "babel src/ --our-dir lib/ --source-maps",
    "build:watch": "babel src/ --out-dir lib/ --source-maps --watch",
    "pub": "node ./scripts/prepublish.js && lerna publish"
  },
  "author": "Chris Pearce <hello@chrispearce.me>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-eslint": "^6.0.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-2": "^6.5.0",
    "eslint": "^2.7.0",
    "eslint-config-airbnb": "^7.0.0",
    "eslint-config-lystable": "^3.0.1",
    "eslint-plugin-flow-vars": "^0.3.0",
    "eslint-plugin-react": "^4.3.0",
    "expect": "^1.16.0",
    "mocha": "^2.4.5"
  }
}
